<h3>Edit product:</h3>
<br><div class="text-center">
	<img src="https://www.lg.com/ru/images/monitors/md05262660/gallery/Global_22-24MP58VQ_2016_Gallery_medium01.jpg">
</div> 
<hr>
<%= form_for :product, url: product_path(@product), method: :patch do |f| %>
	<% if @product.errors.any? %>
		<% @product.errors.full_messages.each do |error_message| %>
		<div class="alert alert-danger">
			<%= error_message %>
		</div>
		<% end %>
	<% end %>
	<p>
		Product title:<br>
		<%= f.text_field(:title, {:class => 'form-control'}) %>
	</p>
	<p>
		Product description:<br>
		<%= f.text_area(:description, {:class => 'form-control'}) %>
	</p>

	<p>
		Price:<br>
		<%= f.text_field(:price, {:class => 'form-control'}) %>
	</p>
	<p>
		<%= f.file_field :image, {:class => 'form-control'} %>
	</p>

	<p>
		<%= f.submit({:class => 'btn btn-success'}) %>
	</p>
<% end %>